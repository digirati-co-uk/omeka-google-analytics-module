language: php
sudo: false
cache:
    directories:
        - vendor
        - $HOME/.composer/cache
php:
    - 5.6
    - 7.0
    - 7.1

install:
    - travis_retry composer install
script:
    - ./vendor/bin/phpunit -c ./phpunit.xml -v
